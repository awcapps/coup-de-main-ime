<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme - Mode Clair -->
    <style name="Theme.CoupDeMain" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_secondary</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Surface colors -->
        <item name="colorSurface">@color/surface_color</item>
        <item name="colorOnSurface">@color/text_primary</item>
        
        <!-- Background colors -->
        <item name="android:windowBackground">@color/background_primary</item>
        <item name="backgroundColor">@color/background_primary</item>
        
        <!-- Status bar -->
        <item name="android:statusBarColor">@color/color_primary</item>
    </style>

    <!-- Styles pour les boutons du clavier IME (couleurs appliquées dynamiquement) -->
    <style name="KeyboardButton" parent="Widget.Material3.Button.TextButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">52dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">1dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <style name="KeyboardButton.Wide">
        <item name="android:layout_weight">1.5</item>
    </style>

    <style name="KeyboardButton.ExtraWide">
        <item name="android:layout_weight">3</item>
    </style>

</resources>
